{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel"
  },
  "license": "ISC",
  "dependencies": {
    "turbo": "^1.6.3"
  }
}
