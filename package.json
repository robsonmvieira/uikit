{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run prod",
    "dev": "turbo run dev --parallel"
  },
  "license": "ISC",
  "dependencies": {
    "turbo": "^1.6.3"
  }
}
